buildscript {
    dependencies {
        classpath "org.springframework.boot:spring-boot-gradle-plugin:3.1.2"
    }
}

allprojects {
    group = 'org.example'
    version = '1.0.0'
    repositories {
        mavenCentral()
    }
}
